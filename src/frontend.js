/**
 * Frontend
 */

import inView from 'in-view';

const animateBlocks = Array.from( document.querySelectorAll( '.gutenberg-animate-block' ) );
// todo, foreach over and set up watcher
// Pass attributes as json
// inView(  )
// 	.on( 'enter', el => {
// 		el.classList.add( el.dataset.in );
// 	} )
// 	.on( 'exit', el => {
// 		el.classList.add( el.dataset.out );
// 	} );
